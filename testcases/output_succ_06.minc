Success: no syntax error is found.

Following is the indentation-updated source code:

int main(int a)
{
    int x;
    x = x + 1;
    while( 1 == 2 )
    {
        int y;
        x = x + 2;
    }
}

int main(int a)
{
    int x;
    x = x + 1;
    while( 1 == 2 )
    {
        int y;
        x = x + 2;
    }
}

